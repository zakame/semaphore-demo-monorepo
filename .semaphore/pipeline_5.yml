version: v1.0
name: Stage ui
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - echo "job 1"
promotions:
  - name: Deploy ui
    pipeline_file: pipeline_9.yml
    auto_promote:
      when: branch = 'master' AND result = 'passed'
